{
  "version": 2,
  "builds": [
    {
      "src": "Server/app.mjs",
      "use": "@vercel/node",
      "config": { "includeFiles": ["Server/**"] }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "Server/app.mjs"
    }
  ]
}
